

async function initializeDB(app) {
    const { Program, Shared, Eligibility } = app.models;
}

async function cleanupDB(app) {
    const { Program, Shared, Eligibility } = app.models;
}

module.exports = { initializeDB, cleanupDB };

